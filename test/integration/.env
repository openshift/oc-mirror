export CONSOLE_REDHAT_COM_PULL_SECRET="$(cat /etc/pull-secret/.dockerconfigjson)"
export AWS_ACCESS_KEY_ID="$(cat /aws-creds/AWS_ACCESS_KEY_ID)"
export AWS_SECRET_ACCESS_KEY="$(cat /aws-creds/AWS_SECRET_ACCESS_KEY)"
